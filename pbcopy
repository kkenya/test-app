# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:$HOME/.local/bin:$HOME/bin

export PATH

# rbenv
export PATH="$HOME/.rbenv/bin:$PATH"
export RAILS_ENV=production

# rails 
export TEST_APP_DB_NAME=rails_test_app_db
export TEST_APP_DB_USERNAME=Administrator
export TEST_APP_DB_PASSWORD=nnjkrsbs
export TEST_APP_DB_ENDPOINT="rails-test-app-db.cnw9svfxe9cp.us-east-2.rds.amazonaws.com"
